/**
 * React Query hooks for backend integration.
 * Currently, the app uses client-side nutrition API calls.
 * This file is reserved for future backend queries (e.g., syncing meals to IC).
 */

// Placeholder for future backend queries
export {};
